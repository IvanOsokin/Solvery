#include "TextFile.h"

/*TextFile::TextFile(const fs::path& fileName)
{
	if (!fs::exists(fileName))
		throw std::runtime_error(std::string("File \"" + fileName.string() + "\" does not found!"));

	;
}*/